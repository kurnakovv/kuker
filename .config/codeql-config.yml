name: "Custom CodeQL Configuration for C#"

languages: [ "actions", "csharp" ]

queries:
  - uses: security-and-quality
    exclude:
      - cs/linq/missed-where

query-filters:
  - exclude:
      tags: /cwe-275/
  - exclude:
      tags: /cwe-829/
